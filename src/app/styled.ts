import { css } from '@emotion/react';

export const baseStyles = css`
  body {
    width: 630px;
    height: 492px;
    margin: 0;
    display: flex;
  }

  #app-container {
    flex: 1;
  }

  * {
    box-sizing: border-box;
  }
`;
